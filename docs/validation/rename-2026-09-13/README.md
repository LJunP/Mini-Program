# 目录与仓库改名验证（2026-09-13）

## 已完成

- 资源仓库 origin 为 https://github.com/LJunP/Mini-Program-data.git。
- 小程序三个环境使用新的 Gitee 仓库路径。
- 31 个脚本中的旧绝对路径改为按脚本位置定位项目。
- 实际运行发现浏览历史页把 Promise 当数组使用；已改为等待异步列表，并防止过期请求及卸载后回写。

## 验证结果

- 微信开发者工具成功打开新目录；32 个已注册页面全部完成入口导航检查（含两种学习详情，共 33 个导航场景）。
- 首轮发现历史页 historyList.map 错误，修复后相关页面重验未捕获 JavaScript 异常。
- 茶列表加载 20 条（首屏分页），行旅 6 条，音乐详情及音频地址存在。
- 真实 getStudyIndex 云函数 code=0，18 个专题、901 道题。
- 119 个去重内容素材的云端临时地址全部返回成功。
- 真实图片解码为 750×560；一段音频实际下载 HTTP 200。
- 微信运行环境访问新 Gitee README 返回 HTTP 200，且内容为 README。
- 前序验证：Gitee 158 个资源内容与 Git 中资源一致；GitHub 与 Gitee HEAD 一致。
- 修复后 10 个本地测试脚本全部通过，git diff --check 通过。

## 验证边界

以上是开发者工具运行与本地测试证据，不是物理手机全功能验收。
没有执行投稿发布、删除记录、支付等写操作验收，没有提交、推送或发布小程序。
页面导航成功不等于每个按钮和所有账号状态均已验证。原有尚无音频的曲目仍为音频筹备中，不属于改名引入的故障。

## 原始记录

- runtime-report.json：第一组页面和真实题库请求；茶和行旅首次采样时尚在加载，其完成状态见 resource-report.json。
- resource-report.json：素材批次、实际下载、新 Gitee 运行时请求及页面复查。
- remaining-report.json：修复历史页之后的其余页面复查。
