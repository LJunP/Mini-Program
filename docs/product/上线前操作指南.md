# 妙不可园 · 上线前操作指南

> **更新日期**：2026年7月24日
> **用途**：从云环境初始化到提审发布的全套操作指南，按先后顺序排列

---

## 操作顺序总览

```text
第 1 步：开通腾讯云 TTS 服务（腾讯云控制台网页）
    ↓
第 2 步：核对云环境 ID + 创建云数据库集合（微信开发者工具）
    ↓
第 3 步：上传部署全部 7 个云函数（微信开发者工具）
    ↓
第 4 步：配置云函数环境变量与超时时间（云开发控制台）
    ↓
第 5 步：配置隐私声明 + 服务器域名白名单（微信公众平台）
    ↓
第 6 步：复制图片素材到本地目录（终端命令）
    ↓
第 7 步：确认 CDN 图片可访问（浏览器验证）
    ↓
第 8 步：补充音乐音频文件（可选，不影响提审）
    ↓
第 9 步：全流程功能验证测试（开发者工具 / 真机）
    ↓
第 10 步：常见报错排查 + 版本提审发布
```

---

## 第 1 步：开通腾讯云 TTS 服务

> ⏱️ 约 3 分钟 | 网页操作

1. 浏览器访问 [腾讯云 TTS 语音合成控制台](https://console.cloud.tencent.com/tts)
2. 确认页面显示"已开通"。若未开通，点击"立即开通"（个人开发者每月 **500 万字符免费额度**）
3. 登录 [腾讯云 API 密钥管理](https://console.cloud.tencent.com/cam/capi)，获取：
   - **SecretId**：`<从腾讯云密钥管理复制，仅配置到云函数环境变量>`
   - **SecretKey**：*你自己保管的那串私钥*（切勿硬编码入代码仓库）
4. 确认授权策略为 `QcloudTTSFullAccess`

> ⚠️ 如提示需要实名认证，请先完成个人实名认证。

**✅ 判断成功**：页面能看到"已开通"字样或用量统计页面。

---

## 第 2 步：核对云环境 ID + 创建云数据库集合

> ⏱️ 约 5 分钟 | 微信开发者工具

### 2.1 核对云环境 ID
1. 微信开发者工具 → 点击顶部 **「云开发」** 按钮
2. 查看控制台左上角的 **环境 ID**
3. 打开 `miniapp/app.js` 第 121 行，确保 `env` 值与控制台环境 ID 一致：
   ```javascript
   wx.cloud.init({ env: '你的云开发环境ID', traceUser: true })
   ```

### 2.2 创建 4 个数据库集合
云开发控制台 → 左侧 **「数据库」** → 点 **「+」** 逐个创建：

| # | 集合名 | 用途 | 权限 |
|---|---|---|---|
| 1 | `users` | 用户信息与偏好 | 仅创建者可读写 |
| 2 | `sign_records` | 签到记录 | 仅创建者可读写 |
| 3 | `collections` | 收藏记录 | 仅创建者可读写 |
| 4 | `events` | 行为埋点 | 仅创建者可写 |

**权限设置**：点击集合名 → 右上角 **「权限设置」** → 选择对应权限 → 保存。

### 2.3（推荐）创建数据库索引
- `collections`：索引 `idx_openid_type`，字段 `_openid`(ASC) + `targetType`(ASC)
- `sign_records`：索引 `idx_openid_date`，字段 `_openid`(ASC) + `signDate`(DESC)
- `events`：索引 `idx_openid_time`，字段 `_openid`(ASC) + `timestamp`(DESC)

**✅ 判断成功**：左侧集合列表能看到 4 个集合名字。

---

## 第 3 步：上传部署全部 7 个云函数

> ⏱️ 约 10 分钟 | 微信开发者工具

左侧文件树展开 `cloudfunctions/`，对每个文件夹执行：**右键 → 「上传并部署：云端安装依赖（不上传 node_modules）」**

| # | 云函数 | 功能 | 超时 |
|---|---|---|---|
| 1 | `login` | 用户 openid 获取与静默登录 | 5 秒 |
| 2 | `collection` | 收藏夹同步 | 5 秒 |
| 3 | `sign` | 签到打卡与连续天数 | 5 秒 |
| 4 | `updateProfile` | 用户资料更新 | 5 秒 |
| 5 | `tts` | 腾讯云 TTS 语音合成 | **10 秒** |
| 6 | `track` | 行为埋点上报 | 5 秒 |
| 7 | `getStudyData` | 拉取解密全量 901 题库 | **15 秒** |

> ✅ 每个部署提示"上传云函数成功"后，再进行下一个。

**✅ 判断成功**：云开发控制台 → 云函数列表中能看到 7 个云函数。
**如果报错**"模块安装失败"→ 再右键重试一次；"文件过大"→ 检查是否有多余的 `node_modules` 目录，删除后重试。

---

## 第 4 步：配置云函数环境变量与超时

> ⏱️ 约 3 分钟 | 云开发控制台

回到云开发控制台 → **「云函数」** → **「云函数列表」** 标签页。

### 4.1 配置 `tts` 云函数
1. 点击 `tts` 进入详情 → `$LATEST` 行右侧 **「配置」** 按钮
2. **环境变量** → 新增：
   - `TENCENT_SECRET_ID`：`<从腾讯云密钥管理复制，仅配置到云函数环境变量>`
   - `TENCENT_SECRET_KEY`：*你自己保管的 SecretKey*
3. **超时时间** → 改为 **10 秒** → 保存

### 4.2 配置 `getStudyData` 云函数
1. 返回列表 → 点击 `getStudyData` → **「配置」**
2. **环境变量** → 新增：
   - `STUDY_DATA_KEY`：`<使用密码管理器生成的随机密钥，不得写入仓库>`
   - `STUDY_DATA_URL`：`https://cdn.jsdelivr.net/gh/lijunpeng/workbuddy_project/data/study_data.enc`
   - *(备用直链：`https://gitee.com/LJunP/workbuddy-assets/raw/main/data/study_data.enc`)*
3. **超时时间** → 改为 **15 秒** → 保存（⚠️ 极重要，防止默认 3 秒超时报错）

**✅ 判断成功**：`tts` 和 `getStudyData` 的环境变量列表各显示 2 条记录。

---

## 第 5 步：配置隐私声明 + 域名白名单

> ⏱️ 约 10 分钟 | 微信公众平台 (mp.weixin.qq.com)

### 5.1 隐私保护指引
1. 登录 [微信公众平台](https://mp.weixin.qq.com)
2. **「设置」** → **「服务内容声明」** → **「用户隐私保护指引」** → 点击「去完善」
3. 勾选并填写用途：
   - **微信昵称头像** → `用于在个人中心及打卡记录中展示用户头像与昵称`
   - **相册（写入）** → `用于保存生成的风雅卡片和海报图片到手机相册`
   - **设备信息** → `用于优化小程序页面适配及崩溃问题排查`
4. 数据接收方填"无"，联系邮箱填你的邮箱
5. 勾选同意须知 → **「提交」**

### 5.2 服务器域名白名单
**「开发」** → **「开发管理」** → **「开发设置」** → **「服务器域名」**：
- **request 合法域名** + **downloadFile 合法域名** 添加：
  - `https://gitee.com`（CDN 图片与素材）
  - `https://cdn.jsdelivr.net`（加密题库拉取）

**✅ 判断成功**：提交后显示"提交成功，等待审核"（审核通常 1-2 小时内自动通过）。

---

## 第 6 步：复制图片素材到本地目录

> ⏱️ 约 1 分钟 | 终端命令

```bash
mkdir -p /Users/lijunpeng/Desktop/workbuddy_project/miniapp/assets/images/
cp -r /Users/lijunpeng/Desktop/workbuddy_project/data/cdn_backup/assets/images/* /Users/lijunpeng/Desktop/workbuddy_project/miniapp/assets/images/
```

执行后在微信开发者工具中重新编译，本地模拟器即可预览完整图片。

**✅ 判断成功**：`miniapp/assets/images/` 下能看到 `tea`、`incense`、`music` 等子文件夹。

---

## 第 7 步：确认 CDN 图片可访问

> ⏱️ 约 5 分钟 | 浏览器验证

在浏览器中打开以下链接验证 Gitee 仓库图片可访问：

1. 茶：`https://gitee.com/LJunP/workbuddy-assets/raw/main/assets/images/tea/longjing.jpg`
2. 香：`https://gitee.com/LJunP/workbuddy-assets/raw/main/assets/images/incense/chenxiang.jpg`
3. 音：`https://gitee.com/LJunP/workbuddy-assets/raw/main/assets/images/music/gaoshan.jpg`

> 💡 若 404：将本地 `data/cdn_backup/assets/images/` 下所有图片推送到 Gitee 仓库 `LJunP/workbuddy-assets` 的 `assets/images/` 对应路径。

---

## 第 8 步：补充音乐音频（可选）

> 不影响提审，有了音频素材再做

1. 将 MP3 上传至 Gitee 仓库 `LJunP/workbuddy-assets` 的 `assets/audio/` 目录
2. 在 `miniapp/utils/data-store.js` 中填入对应 `audioSrc` 字段

| 曲名 | CDN 路径 |
|---|---|
| 《流水》 | `assets/audio/liushui.mp3` |
| 《渔舟唱晚》 | `assets/audio/yuzhou-changwan.mp3` |
| 《二泉映月》 | `assets/audio/erquan-yingyue.mp3` |
| 《春江花月夜》 | `assets/audio/chunjiang-huayueye.mp3` |
| 《十面埋伏》 | `assets/audio/shimian-maifu.mp3` |
| 《梅花三弄》 | `assets/audio/meihua-sannong.mp3` |
| 《百鸟朝凤》 | `assets/audio/bainiao-zhaofeng.mp3` |
| 《平沙落雁》 | `assets/audio/pingsha-luoyan.mp3` |
| 《阳关三叠》 | `assets/audio/yangguan-sandie.mp3` |
| 《高山》 | `assets/audio/gaoshan.mp3` |
| 《琵琶行》 | `assets/audio/pipaxing.mp3` |
| 《渔樵问答》 | `assets/audio/yuqiao-wenda.mp3` |

---

## 第 9 步：全流程验证测试

> ⏱️ 约 15 分钟 | 开发者工具 + 真机预览

### 基础 UI 验证
| # | 测试项 | 操作 | 预期结果 |
|---|---|---|---|
| 1 | 启动 | 点「重新编译」 | 无报错，首页流畅渲染 |
| 2 | 素材显示 | 浏览六雅模块列表 | 封面图片正常加载 |
| 3 | 内容详情 | 点任意卡片进详情页 | 排版无错乱 |
| 4 | 茶品雷达图 | 进茶品详情页 | Canvas 5 维雷达图正常 |

### 云函数验证
| # | 测试项 | 操作 | 预期结果 |
|---|---|---|---|
| 5 | 登录 | 查看 Console | `[login] success` 无 error |
| 6 | 收藏 | 详情页点「收藏」 | 提示"已收藏"，书签页可见 |
| 7 | 签到 | 个人中心点「签到」 | 打卡成功，连续天数增加 |
| 8 | 资料 | 编辑昵称/签名保存 | 刷新后保留 |

### TTS 验证
| # | 测试项 | 操作 | 预期结果 |
|---|---|---|---|
| 9 | TTS 触发 | 面试题详情页点「朗读」 | Toast "正在合成语音…" |
| 10 | 播放 | 等待 1-3 秒 | 清晰播放语音 |
| 11 | 停止 | 切换离开页面 | 语音自动停止 |

### 数据验证
| # | 测试项 | 操作 | 预期结果 |
|---|---|---|---|
| 12 | 埋点 | 浏览 3-5 个页面 | `events` 集合新增记录 |
| 13 | 题库 | 学习 → 面试题库 | 题目总数 901 道（非 90 道） |

---

## 第 10 步：常见报错排查 + 提审发布

### 常见问题

| 报错 | 原因 | 解决 |
|---|---|---|
| `getStudyData` timeout | 默认超时 3 秒 | 改为 **15 秒** |
| `tts` AuthFailure | 密钥配置有误 | 检查环境变量是否与 CAM 一致 |
| `Permission Denied` | 集合权限不对 | 改为"仅创建者可读写" |
| 主包超过 2048KB | 本地图片过大 | 线上版本用 CDN 动态加载，移出大图 |

### 提审发布步骤

1. 微信开发者工具右上角 **「上传」**
2. 版本号 `1.0.0`，备注 `妙不可园正式上线版本`
3. [公众平台](https://mp.weixin.qq.com) → **「版本管理」** → **「开发版本」** → **「提交审核」**
4. 审核通过后 → **「发布」** → 全网上线

---

## 完成确认 Check List

- [ ] TTS 已开通，密钥已保存
- [ ] 云环境 ID 已对齐，4 个数据库集合已创建
- [ ] 7 个云函数已部署成功
- [ ] `tts` + `getStudyData` 环境变量与超时已配置
- [ ] 隐私指引与域名白名单已提交
- [ ] 本地 116 张封面图片已复制
- [ ] CDN 图片链接可正常访问
- [ ] 13 项功能测试全部通过
- [ ] 代码已上传，准备提审
